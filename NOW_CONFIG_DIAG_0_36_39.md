# 0.36.39 NOW config diagnostic hotfix

- Fixes lost `missingEnv` details on failed NOW refresh start response.
- Admin alert now displays the exact missing NAVER environment variable names.
- Keeps NOW collection/scoring logic unchanged.
- Keeps `BUILD_NAME` export intact.

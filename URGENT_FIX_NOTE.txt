JCV3 URGENT NOW STORAGE SINGLE-VALUE ROOTFIX — 2026-08-30

Root cause:
- Upstash request limit is ~10 MiB.
- Previous MSET splitting could not help when the single CURRENT value itself exceeded the limit.
- Large repeated news links inside 542 x headline arrays pushed CURRENT over the request ceiling.

Fix:
- Store a compact CURRENT snapshot that preserves ranks, people, scores, counts, latest timestamp, headline title/source/time.
- Drop only oversized headline link payloads from the compatibility CURRENT snapshot.
- Full per-person public snapshots and JCS/AGE-GENDER calculations still use the complete in-memory current during publish.
- AGE/GENDER engine files are untouched.

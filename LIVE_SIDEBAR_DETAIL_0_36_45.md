# 정참시 v3 · 0.36.45
## LIVE SIGNAL SIDEBAR + POLITICIAN LIVE DETAIL

### 1. 오른쪽 사이드바 · 실시간 정치키워드
- 관리자에서 게시된 `nowDataCurrent` 스냅샷의 최근 뉴스 제목을 자동 분석합니다.
- 상위 NOW 정치인 뉴스에 더 높은 가중치를 주고, 6시간/24시간/7일 신선도 가중치를 반영합니다.
- 정치인 본인 이름과 일반 불용어를 제거하고 실제 반복 이슈를 TOP 15로 생성합니다.
- 메인 오른쪽 사이드바에는 TOP 8을 표시합니다.
- `/keywords` 전체 페이지도 같은 실데이터를 사용합니다.

### 2. 오른쪽 사이드바 · 실시간 급상승 정치인
- 현재 게시 스냅샷과 직전 게시 스냅샷의 TOP30을 비교합니다.
- 순위 상승폭이 있으면 `▲ n`을 최우선으로 표시합니다.
- 새 TOP30 진입은 `NEW`로 표시합니다.
- 첫 게시처럼 비교 스냅샷이 없으면 최근 6시간 뉴스 가속도를 기준으로 급상승 후보를 구성합니다.
- 메인 오른쪽 사이드바 TOP5, `/trending` 전체 페이지 TOP10을 사용합니다.

### 3. 정치인 상세페이지 · 자유의지 LIVE PROFILE
상세페이지 정보 위계를 전면 재구성했습니다.

1. 인물 정체성 / 현재 직책
2. NOW Rank / 순위 변동 / NOW Score
3. `지금 이 인물`
4. `왜 지금 주목받나요?`
5. 네이버 PC 검색량 / 모바일 검색량 / 월간 합계
6. 최근 6시간 / 24시간 / 7일 뉴스량 / 출처 다양성
7. 최근 뉴스 최대 6건
8. 기본 프로필 / 임기·선거정보
9. 정치 타임라인
10. 같은 정당·지역·NOW 인접 정치인 추천

### 데이터 원칙
- 사이드바와 상세페이지는 모두 관리자가 게시한 동일한 NOW 스냅샷을 원천으로 사용합니다.
- 검색량은 네이버 검색광고 수집 데이터입니다.
- 뉴스는 현재 수집 엔진의 실제 provider를 그대로 보존합니다. 네이버 뉴스 키가 없으면 Google News RSS fallback 결과가 게시 스냅샷에 들어갑니다.
- 공개용 `/api/v3/now-data`를 추가하여 상세/키워드/급상승이 무거운 전체 홈 데이터를 다시 받지 않도록 분리했습니다.

### 버전
- APP_VERSION: `v3.0.0-alpha6.0.36.45`
- ADMIN_VERSION: `0.36.45`
- BUILD_NAME: `LIVE SIGNAL SIDEBAR + POLITICIAN LIVE DETAIL`

### 검증
- 프론트/서버 주요 JS `node --check` 통과
- `npm test`: 30/30 통과
- NOW signal smoke: keywords/rising 생성 확인

const { getJSON }=require('../../../lib/v3/redis');
const { defaultDomain }=require('../../../lib/v3/schema');
const { listUsers }=require('../../../lib/v3/users');
const { getRecentBadgeCelebrations }=require('../../../lib/v3/badge-celebrations');
module.exports=async function handler(req,res){res.setHeader('Content-Type','application/json; charset=utf-8');if(req.method!=='GET'){res.setHeader('Allow','GET');return res.status(405).json({ok:false,error:'METHOD_NOT_ALLOWED'});}try{const [brandValue,users,badgeCelebrations]=await Promise.all([getJSON('brand'),listUsers(),getRecentBadgeCelebrations(8)]),brand=brandValue||defaultDomain('brand'),liveBar={useActualCount:true,overrideCount:0,...(brand?.liveBar||{})};res.setHeader('Cache-Control','public, max-age=0, s-maxage=5, stale-while-revalidate=10');return res.status(200).json({ok:true,data:{memberCount:Array.isArray(users)?users.length:0,liveBar,badgeCelebrations}});}catch(error){return res.status(error?.code==='STORAGE_MISSING'?503:500).json({ok:false,error:error?.code||'LIVEBAR_READ_FAILED'});}};

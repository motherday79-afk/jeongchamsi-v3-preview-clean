JCV3 JCS AGE/GENDER COHORT FLATTENING HOTFIX R2
Date: 2026-08-30

ROOT CAUSE FIXED
- The first calculation hotfix corrected the cohort formula, but the admin reader could still serve the previously frozen immutable V2 snapshot.
- The existing V2 snapshot key/index had no calculation revision, so same-draft recalculation was blocked by SET NX and old flat values remained visible.

R2 FIX
1. Keeps the corrected cohort calculation in age-gender-cohort-core.js.
2. Adds calculation revision COHORT_UNFLATTEN_20260830_R2.
3. Writes corrected V2 snapshots to a revision-specific immutable key and active index.
4. Legacy flat snapshots remain stored for HISTORY but are not used as the active latest/prior for R2 calculations.
5. The same draft can receive a new R2 snapshot without overwriting the historical legacy snapshot.

FOCUSED VERIFICATION
- political-intelligence-age-gender-v2.test.js: 18/18 PASS
- political-intelligence-v2-store-20260830.test.js: 5/5 PASS
- Total focused: 23/23 PASS
- JS syntax: PASS

REGRESSION GUARDS
- High common momentum cannot flatten a non-flat official 12-cell baseline into one repeated score.
- A previously flattened V2 snapshot cannot permanently lock later cohort values together.
- A same-draft legacy flat snapshot cannot block creation/read of the R2 corrected calculation snapshot.

DEPLOYMENT NOTE
- Apply this hotfix over the current project, then run 관리자 전체 데이터 새로고침 once so the R2 active snapshot is generated from the current official baseline.

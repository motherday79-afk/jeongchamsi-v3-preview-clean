# 0.36.67 POLITICIAN PHOTO COLLECTION STAGE 2

## 목적
기존 Wikimedia 엄격 자동수집에서 자산화되지 않은 정치인을 국회·지자체 공식기관 웹문서까지 확장 탐색하고, 자동확정이 어려운 사진을 관리자 후보 검수함으로 모아 수기 검색량을 줄입니다.

## 핵심 동작
- 기존 수기등록 및 정참시 사진 자산은 자동수집이 절대 덮어쓰지 않습니다.
- Wikimedia에서 이름 + 정당 + 직위 + 지역 문맥이 모두 확인되면 기존 방식대로 즉시 정참시 Blob 자산으로 저장합니다.
- Wikimedia에서 확정하지 못하면 공식기관 후보를 추가 탐색합니다.
- 국회의원은 `ASSEMBLY_OPENAPI_KEY`가 설정된 경우 열린국회정보 `ALLNAMEMBER`의 이름·정당·선거구·사진 필드를 추가 후보로 사용합니다.
- 키가 없어도 기존 `NAVER_API_HUB_CLIENT_ID` / `NAVER_API_HUB_CLIENT_SECRET`을 재사용해 국회 및 `*.go.kr` 공식 프로필 웹문서를 찾습니다.
- 공식 페이지에서도 이름 + 정당 + 직위 + 지역이 모두 확인되어야 후보로 등록됩니다.
- 공식 웹 후보는 자동확정하지 않고 정치인별 최대 3장을 `후보 검수함`에 표시합니다.
- 관리자가 출처와 이용조건을 확인한 뒤 `이 사진 적용`을 누르면 기존 MINI/CARD/PROFILE 최적화 파이프를 그대로 거쳐 총 128KB 상한으로 정참시 Blob 자산화합니다.

## 관리자 상태 분류
- 정참시 자산
- 후보발견 / 검토필요
- 신원불일치
- 사진없음
- 수집오류: 소스 조회, 이미지 조회, 이미지 용량, Blob 업로드 실패
- 미확인

후보 이미지 처리 중 실패가 발생해도 후보 자체는 검수함에서 지우지 않고 마지막 실패 원인을 별도로 기록합니다. 다른 후보를 선택해 다시 시도할 수 있습니다.

## 관리자 사용 순서
1. `관리자 > 인물 관리`에서 `2단계 자동수집 시작`을 실행합니다.
2. 수집이 끝나면 `후보 검수함`의 정치인별 후보사진과 `출처 확인`을 검토합니다.
3. 사용 가능한 사진은 `이 사진 적용`으로 정참시 자산화합니다.
4. `사진없음`, `신원불일치`, 반복 실패 인원만 기존 수기등록 기능으로 보완합니다.

## 환경변수
필수 신규 환경변수는 없습니다. 기존 Naver 뉴스 API HUB 자격증명을 웹문서 탐색에도 재사용합니다.

- 기존 사용: `NAVER_API_HUB_CLIENT_ID`
- 기존 사용: `NAVER_API_HUB_CLIENT_SECRET`
- 선택 사용: `ASSEMBLY_OPENAPI_KEY`

`ASSEMBLY_OPENAPI_KEY`가 없어도 Naver 공식 웹문서 탐색은 동작합니다.

## 신뢰 안전장치
박성민처럼 동명이인이 있는 경우 이름만으로 통과시키지 않습니다. 정당, 직위, 지역 문맥이 모두 맞지 않으면 자동확정 및 공식 후보 등록에서 탈락합니다. 기존 자산이 있는 정치인은 2단계 수집과 후보 승인 모두 덮어쓰지 않습니다.

## 실제 확보량
개발 컨테이너에서는 외부 공식기관/Naver API를 실제 542명 전체 대상으로 실행할 수 없으므로, 29명에서 최종 몇 명까지 늘어나는지는 배포 후 `2단계 자동수집 시작`을 실제 실행한 결과로 확인해야 합니다.

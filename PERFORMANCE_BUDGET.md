# 정참시 v3 성능 헌법

## 제1원칙
**첫째도 속도, 둘째도 속도. 사용자가 기다리지 않게 만든다.**

기능 추가가 아래 기준을 깨면 기능을 그대로 합치지 않는다.
지연 로딩, 코드 분리, 캐시, 사전 계산, 별도 페이지 분리를 먼저 적용한다.

## v3 성능 예산

| 항목 | 목표 | 경고선 |
|---|---:|---:|
| TTFB | 500ms 이하 | 800ms |
| FCP | 1.0초 이하 | 1.5초 |
| LCP | 1.8초 이하 | 2.5초 |
| CLS | 0.05 이하 | 0.10 |
| 초기 요청 수 | 25개 이하 | 40개 |
| 초기 JS 전송량 | 60KB 이하 | 100KB |
| 초기 CSS 전송량 | 40KB 이하 | 70KB |
| 첫 화면 이미지 전송량 | 400KB 이하 | 800KB |
| 초기 렌더를 막는 API | 0개 | 1개 이상 금지 |

## 개발 규칙

1. HOME은 포털 입구다. 상세 데이터 전체를 최초 로딩에서 가져오지 않는다.
2. 화면 아래 콘텐츠와 이미지는 지연 로딩한다.
3. 정치인/칼럼 이미지는 CDN 캐시를 우선하고, 변경 시 URL 자체를 바꾼다.
4. 동일 이미지를 새로고침마다 다시 다운로드하지 않는다.
5. 검색·통계·랭킹의 무거운 계산은 요청 때마다 반복하지 않는다.
6. 페이지별 JavaScript를 분리하고 필요한 페이지에서만 불러온다.
7. 공통 상태와 화면 상태를 분리한다. DOM 직접 조작으로 다른 화면을 갱신하지 않는다.
8. 매 배포 전 성능 예산 초과 여부를 확인한다.
9. 예산을 넘긴 기능은 “완료”로 보지 않는다.
10. 운영 v2는 v3 검증 완료 전까지 건드리지 않는다.

## 검사 방법

Preview URL 뒤에 `?perf=1`을 붙이면 브라우저 우측 하단에
현재 페이지의 TTFB / FCP / LCP / CLS / 요청 수 / 전송량이 표시된다.

예:
`https://<v3-preview-domain>/?perf=1`
